// Dependencies
import { connect } from 'react-redux';

// Shared Components
import BaseComponent from '@baseComponent';

// Components
import Layout from '../components/Layout';

// Actions

export default connect()(BaseComponent(Layout));
