@import '../../../shared/styles/_dependencies'

.Profile
    border-radius 4px
    box-shadow: 0px 2px 7px 0px rgba($colors.common.black,.5)
    display flex
    padding 20px

    img
        width 100px
        display block
        border-radius 5px
        margin-right 10px

    .information
        h2
            fontSize(21px)

        span
            color($colors.common.gray.nine)
            fontSize(13px)
